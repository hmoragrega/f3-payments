version: 2
jobs:
  build:
    working_directory: /go/src/github.com/{{ORG_NAME}}/{{REPO_NAME}}
    docker:
      - image: hmoragrega/f3-payments-ci:1.0.1
    steps:
      - checkout
      - run: make dev
      - run: make mongo-wait-ready
      - run: make mongo-load-fixtures
      - run: make test

workflows:
  version: 2
  ci:
    jobs:
      - build