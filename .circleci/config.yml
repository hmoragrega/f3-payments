version: 2
jobs:
  build:
    docker:
      - image: hmoragrega/f3-payments-ci:1.0.0
    working_directory: /go/src/github.com/{{ORG_NAME}}/{{REPO_NAME}}
    steps:
      - checkout
      - run: make dev
      - run: make mongo-wait-ready
      - run: make mongo-load-fixtures
      - run: make test